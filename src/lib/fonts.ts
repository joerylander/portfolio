import { Inter, Azeret_Mono } from 'next/font/google';

export const inter = Inter({ variable: '--font-inter', subsets: ['latin'] });
export const azaret_mono = Azeret_Mono({
  variable: '--font-azaret_mono',
  subsets: ['latin'],
});
