import { azaret_mono } from '@/lib/fonts';

export default function Footer() {
  return (
    <footer className="mx-6 flex items-center justify-center">
      <h3 className={azaret_mono.className}>Footer from component</h3>
    </footer>
  );
}
